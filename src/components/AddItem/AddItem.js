import React from 'react';

import './AddItem.css';

const AppItem = () => {

    return (
        <div>
            <textarea></textarea>
            <button className='add'>+</button>
        </div>
    );
};

export default AppItem;
